apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "velero-annotations-controller.fullname" . }}-test-connection"
  labels:
{{ include "velero-annotations-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "velero-annotations-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
